<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mashang.mashangdriving.mapper.manager.DrivingCoachTimeScheduleMapper">

    <!-- 通用查询列：和你数据库截图完全一致 -->
    <sql id="selectCoachTimeVo">
        schedule_id, instructor_id, start_time,
        end_time, status, del_flag,
        user_id, person
    </sql>

    <!-- 按年月查询，过滤del_flag=0（正常数据） -->
    <select id="selectCoachTimeByYearAndMonth" resultType="DrivingCoachTimeSchedule">
        select <include refid="selectCoachTimeVo"/>
        from driving_coach_time_schedule
        where del_flag = '0'
        and YEAR(start_time) = #{year}
        and MONTH(start_time) = #{month}
    </select>


</mapper>