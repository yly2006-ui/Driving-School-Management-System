<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mashang.mashangdriving.mapper.manager.InstructorMapper">

    <select id="myInstructor" resultType="allInstructorListVo">
        SELECT
            tor.instructor_id,
            tor.instructor_name,
            tor.teaching_years,
            tor.photo,
            tor.good_subject,
            tor.score
        FROM
            driving_instructor AS tor
        WHERE tor.instructor_id = #{instructorId}
    </select>


    <select id="allnstructorList" resultType="allInstructorListVo">
        SELECT
            tor.instructor_id,
            tor.instructor_name,
            tor.teaching_years,
            tor.photo,
            tor.good_subject,
            tor.score
        FROM
            driving_instructor AS tor
        WHERE
            tor.`status` = '0'
    </select>

    <select id="selectAvailableInstructor" resultType="drivingInstructor">
        SELECT *
        FROM driving_instructor i
        WHERE i.del_flag = '0'
        AND i.status = '0'
        AND NOT EXISTS (
        SELECT 1
        FROM driving_appointment a
        WHERE a.instructor_id = i.instructor_id
        AND a.del_flag = '0'
        AND a.start_time &lt; #{endTime}
        AND a.end_time &gt; #{startTime}
        )
        ORDER BY i.score DESC
        LIMIT 1
    </select>
</mapper>