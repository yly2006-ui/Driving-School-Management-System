<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mashang.mashangdriving.mapper.student.StudentMapper">

    <select id="student" resultType="studentDataOverviewDtlVo">
        SELECT
            stu.student_id,
            stu.avatar,
            sub.subject_id,
            sub.subject_name,
            stu.student_name
        FROM
            driving_student AS stu
                LEFT JOIN
            driving_subject AS sub
            ON
                stu.subject_id = sub.subject_id
        WHERE
            stu.user_id = #{userId}
    </select>
</mapper>